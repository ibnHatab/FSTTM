# Dev convenience only — pyproject.toml is the source of truth.
# Engine + all extras for a full dev box:
#   pip install -e ".[piper,tui,voicefilter,test]"
# plus the contrib packages:
#   pip install -e contrib/hvac -e contrib/dog
#
# CUDA notes:
#  - llama-cpp-python: build from source with GGML_CUDA=ON (see README)
#  - pywhispercpp:     scripts/build-whisper.sh builds it with CUDA
#  - onnxruntime-gpu instead of onnxruntime for piper on CUDA hosts
-e .[piper,tui,voicefilter,test]
